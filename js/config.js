/**
 * NEXUS ENTERPRISE ERP — Client Configuration & API Endpoint Registry
 * Configure the backend API Gateway Ingress URL here.
 */
window.API_BASE = window.API_BASE || 'http://localhost:3000';

console.log(`%c[NEXUS ERP Gateway]%c Connected to ${window.API_BASE}`, 'color: #00f0ff; font-weight: bold;', 'color: #10b981;');
